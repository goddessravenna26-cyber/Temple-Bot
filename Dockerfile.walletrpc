FROM ubuntu:22.04

# Install Monero directly from the secure Linux repository instead of downloading files
RUN apt-get update && apt-get install -y monero && rm -rf /var/lib/apt/lists/*

WORKDIR /srv/xmr

EXPOSE 18083

CMD monero-wallet-rpc \
    --rpc-bind-ip 0.0.0.0 --rpc-bind-port 18083 \
    --daemon-address https://moneroworld.com \
    --wallet-dir /srv/xmr/wallets \
    --disable-rpc-login \
    --generate-from-view-key viewonly_wallet \
    --password-file /dev/null \
    --non-interactive
