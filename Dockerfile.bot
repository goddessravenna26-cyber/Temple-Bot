FROM python:3.12-slim

WORKDIR /app
RUN useradd -m -u 10002 bot && mkdir -p /data && chown bot:bot /data

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY bot.py .
USER bot

CMD ["python", "-u", "bot.py"]
